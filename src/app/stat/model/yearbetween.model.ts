export class YearBetween {
    public yearStart: number;
    public yearEnd: number;

    constructor(
        yearStart: number,
        yearEnd: number
    ) {
        this.yearStart = yearStart;
        this.yearEnd = yearEnd;
    }
}