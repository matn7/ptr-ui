export class DaysBetween {
    public rateDay: number;
    public startDate: number;

    constructor(
        rateDay: number,
        startDate: number
    ) {
        this.rateDay = rateDay;
        this.startDate = startDate;
    }
}